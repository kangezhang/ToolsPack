# -*- mode: python ; coding: utf-8 -*-
# Generated by PyInstaller Deployment Tool
# Date: 2025-10-21 16:28:13

block_cipher = None

a = Analysis(
    ['/Users/kang/Documents/Projects/ToolsPack/pytools_scaffolder.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='pytools_scaffolder',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='/Users/kang/Documents/Projects/ToolsPack/S.icns',
)

coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=False,
    upx_exclude=[],
    name='pytools_scaffolder',
)

app = BUNDLE(
    coll,
    name='pytools_scaffolder.app',
    icon='/Users/kang/Documents/Projects/ToolsPack/S.icns' if '/Users/kang/Documents/Projects/ToolsPack/S.icns' else None,
    bundle_identifier='com.pytools_scaffolder.app',
    info_plist={
        'NSHighResolutionCapable': 'True',
        'CFBundleShortVersionString': '1.0.0',
    },
)
